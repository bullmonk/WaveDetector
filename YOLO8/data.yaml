# Define the paths to your training and validation datasets
path: ./test  # Base directory where images and labels are stored

train: ./images/train      # Relative path from 'path' to training images
val: ./images/val          # Relative path from 'path' to validation images

# Number of object classes
nc: 1  # Number of classes (modify according to your dataset)

# Class names
names: 
  0: triangle  # Replace 'class1' with your actual class name